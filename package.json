{
  "name": "fast-study",
  "private": true,
  "type": "module",
  "workspaces": ["lib/*"],
  "scripts": {
    "dev": "concurrently -k -t 'HH:mm:ss' -p '[{time}] {name}' -n Backend,Frontend,Downloader,AutoDL,Database -c blue,green,magenta,cyan,yellow \"cd backend && PYTHONUNBUFFERED=1 uv run uvicorn main:app --reload\" \"npm --prefix frontend run dev\" \"npm --prefix downloader/server start\" \"npm --prefix downloader/auto start\" \"cd database && uv run uvicorn main:app --reload --port 8001\"",
    "lint": "eslint . && uvx ruff check backend database lib"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "concurrently": "^9.1.0",
    "eslint": "^10.8.0",
    "globals": "^17.7.0",
    "prettier": "^3.9.6",
    "typescript-eslint": "^8.65.0"
  }
}
